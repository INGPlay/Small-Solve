# unzip_plus
```bash
python unzip_plus.py --zip_file target_zip_paths --process_count process_for_multiprocessing
```
  
## flag  
--zip_file, -z
--process_count, -pc, 4
  
## feature  
병렬처리로 속도를 높여 압축해제하기